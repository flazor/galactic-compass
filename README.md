# tilt-meter
Visualize how we're flying through space at full tilt - showing Earth's motion vectors at multiple scales

### Test locally
http-server -S -C ~/.httpd_conf/cert.pem -K ~/.httpd_conf/key.pem -c-1
