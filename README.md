# tilt-meter
Visualize how you're flying through space at full tilt - showing your motion vectors at multiple cosmic scales

### Test locally
http-server -S -C ~/.httpd_conf/cert.pem -K ~/.httpd_conf/key.pem -c-1
